# torch / torchaudio are installed separately from the CPU wheel index (see the workflow),
# so that pip does not pull the much larger CUDA build.
numpy==1.26.4
requests==2.32.3
silero-vad==5.1.2
speechbrain==1.0.2

# speechbrain 1.0.x still calls hf_hub_download(use_auth_token=...), and
# huggingface_hub removed that argument in 1.0. Without this pin the model fetch
# raises TypeError and every run fails while loading the encoder.
huggingface_hub==0.25.2

# Only used by archive_transcript(), which is inert unless the R2 secrets are
# set. It is listed here rather than installed on demand because the archive is
# the one thing in the pipeline whose failure is silent by design: a missing
# module would just mean transcripts quietly stop being kept. Importing it lazily
# inside the function keeps a misconfigured archive from breaking delivery.
boto3==1.35.36
