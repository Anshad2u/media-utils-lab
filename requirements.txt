# torch / torchaudio are installed separately from the CPU wheel index (see the workflow),
# so that pip does not pull the much larger CUDA build.
numpy==1.26.4
requests==2.32.3
silero-vad==5.1.2
speechbrain==1.0.2

# speechbrain 1.0.x still calls hf_hub_download(use_auth_token=...), and
# huggingface_hub removed that argument in 1.0. Without this pin the model fetch
# raises TypeError and every run fails while loading the encoder.
huggingface_hub==0.25.2

# The transcript archive is written with git over ssh, against a private
# repository, using the deploy key in ARCHIVE_SSH_KEY. Both are present on the
# ubuntu-latest image, so there is no archive-specific dependency to install -
# which is the point: nothing in this file can make the archive silently stop
# working, and nothing here needs a credential.
